{
  "entities": {
    "PendingOperation": {
      "title": "Pending Operation",
      "description": "An operation currently active and waiting for finishing details.",
      "type": "object",
      "properties": {
        "opNumber": { "type": "string" },
        "litragem": { "type": "string" },
        "produto": { "type": "string" },
        "linha": { "type": "string" },
        "turno": { "type": "string" },
        "horaInicial": { "type": "string" },
        "carimboInicial": { "type": "string" },
        "localId": { "type": "string" },
        "userId": { "type": "string" }
      },
      "required": ["opNumber", "litragem", "produto", "linha", "turno", "horaInicial", "carimboInicial", "localId", "userId"]
    },
    "AuthProfile": {
      "title": "Auth Profile",
      "description": "Stores user credentials such as password and last password change.",
      "type": "object",
      "properties": {
        "password": { "type": "string" },
        "lastPasswordChange": { "type": "string" }
      },
      "required": ["password", "lastPasswordChange"]
    }
  },
  "firestore": {
    "/authProfiles/{profileId}": {
      "schema": "AuthProfile",
      "description": "Store passwords and last modification dates for profiles."
    },
    "/pendingOperations/{opId}": {
      "schema": "PendingOperation",
      "description": "Store all pending operations across sessions before they are pushed to sheets."
    }
  }
}
